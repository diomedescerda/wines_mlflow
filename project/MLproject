name: wine_random_forest_project

conda_env: conda.yaml

entry_points:
    main:
        command: "python ../app.py"
    train:
        command: "python train.py && python register_best.py"
    serve:
        command: "python ../app.py"
